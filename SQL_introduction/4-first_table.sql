-- Task 4: Create a table named 'first_table' in the database
-- The table have two columns, id (INT) and name (VARCHAR(256))
CREATE TABLE IF NOT EXISTS first_table (
    id INT,
    name VARCHAR(256)
);
